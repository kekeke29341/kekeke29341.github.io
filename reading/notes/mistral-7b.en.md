## What I took

A 7B with GQA and sliding-window attention. A concrete case that architecture changes memory even at small size.

## Where I use it

Tasks that need policy outside the window (long logs, the end of a policy) start with sinks or extension. Short QA often fits this design.

## What I doubt

It is a short report. Training data is not fully specified. I borrow the combination of choices, not the number.

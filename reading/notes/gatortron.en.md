## What I took

An LM trained on a large clinical-note corpus. What I still read is the setting — trained without sending PHI out — not the parameter count.

## Where I use it

If a hospital-LLM table only says “clinical,” I ask whether the corpus is local notes, public notes, or claims text. I do not use GatorTron as another name for a small public model.

## What I doubt

Downstream classification tracks local note style. I do not copy the number into another hospital’s estimate.

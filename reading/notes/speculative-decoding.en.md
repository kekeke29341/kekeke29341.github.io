## What I took

A draft model proposes several tokens; the large model verifies them in parallel. The output distribution stays the large model’s.

## Where I use it

I do not ship an acceleration whose accept rate is not in the log. A domain shift raises rejects and can slow you down.

## What I doubt

Mismatched tokenizers break the implementation. I read the vocab before the speed table.

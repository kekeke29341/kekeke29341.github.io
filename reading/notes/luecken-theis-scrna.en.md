## What I took

A tutorial that writes single-cell analysis in the order you actually run it. The order is the object, not the tool names.

## Where I use it

If an ML methods section omits QC thresholds, normalization, and whether integration ran, I cannot reproduce. A UMAP figure is not the result.

## What I doubt

2019 defaults are not 2026 tools. I keep the order and update the versions.

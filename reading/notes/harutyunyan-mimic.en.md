## What I took

A shared MIMIC time-series benchmark (mortality, decompensation, and others). The floor later papers compare on.

## Where I use it

A “SOTA on MIMIC” line needs this benchmark or a custom extract. If custom, time t and the patient split must be redefined in the body.

## What I doubt

Benchmark preprocessing can erase the clinical question you wanted. I read the remaining variables before the score.

## What I took

MoE simplified to one expert per token (Switch). The habit of splitting total parameters from active parameters starts here.

## Where I use it

I do not borrow a dense-70B estimate for “8x7B.” If the router collapses, sparsity is gone. I put load logs next to the loss.

## What I doubt

Auxiliary losses and capacity are training. Serving is whether it fits and whether it is fast. I do not mix those sentences.
